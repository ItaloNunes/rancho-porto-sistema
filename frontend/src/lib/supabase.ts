// Órfão da época do Supabase Auth (login por e-mail). Login do painel hoje é
// próprio — usuário + senha, JWT emitido pelo backend (ver lib/auth.tsx e
// backend/app/security.py) — nada aqui é importado por ninguém. Mantido só
// pra não sumir do histórico; dá pra apagar o arquivo com segurança.
export {};
