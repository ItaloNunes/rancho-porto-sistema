fastapi==0.115.0
uvicorn[standard]==0.30.6
supabase==2.9.1
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
fpdf2==2.8.8
python-multipart==0.0.12
# Login próprio (usuário + senha), sem Supabase Auth: hash de senha e o JWT
# que o backend emite e valida sozinho — ver app/usuarios.py e app/security.py.
bcrypt==4.2.0
PyJWT==2.9.0
# Relatório completo (proposta + documentos anexados num PDF só, ver
# routers/crm.py::gerar_relatorio_completo / app/pdf.py::montar_relatorio_completo):
# pypdf mescla as páginas, Pillow+pillow-heif convertem cada imagem anexada
# (JPG/PNG/WEBP/HEIC) numa página de PDF antes de mesclar.
pypdf==5.1.0
Pillow==11.0.0
pillow-heif==0.21.0
# Importação de planilha de lotes (painel, só admin) — só .xlsx precisa de
# lib externa, .csv usa o módulo csv da stdlib (ver app/importacao.py).
openpyxl==3.1.5
# Importação de planilha de lotes: além de .xlsx/.csv, aceita a tabela
# oficial em PDF da construtora (status vem só como cor de fundo da linha,
# não como texto — ver app/importacao.py::ler_planilha_pdf).
pdfplumber==0.11.4
pymupdf==1.24.11
httpx==0.27.2  # Chamada HTTP à API do Resend (envio de e-mail — ver app/email_service.py)
